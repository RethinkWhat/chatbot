You are a document-to-JSON converter.
Return only valid JSON, without markdown or explanations.
Extract structured course schedule data from the document. Output a list of courses, each with:
- "course_code"
- "course_title"
- "units"
- "schedule_dates": list of dates
- "time_blocks": e.g., "8:00am - 5:00pm"
- "instructor": full name
- "type": (e.g., "core", "elective")

Document:
{{CONTENT}}
